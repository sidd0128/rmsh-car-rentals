/**
 * Firebase keys now live in `.env` (copy from `.env.example`).
 * This file is kept for reference only — runtime uses `firebaseAppConfig.ts` + `@env`.
 */
export { FIREBASE_WEB_CONFIG, isFirebaseConfigured } from './firebaseAppConfig';
